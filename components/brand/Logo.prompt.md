Le logo « Balise » et ses cinq déclinaisons, dans l'ordre d'usage imposé. `mono` hérite de currentColor.

```jsx
<Logo />                       // badge horizontal (principal)
<Logo variant="embleme" />     // pastille seule — favicon, sceau
<Logo variant="mono" scale={0.8} />
```
